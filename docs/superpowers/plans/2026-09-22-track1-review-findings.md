88 must/should findings from 83 distinct issues (unverified: reviewers only, no skeptic pass)

### [should_fix] SKILL.md step 3 still describes a screenshot per step taken before the decision  (SKILL.md; reported 3x)
SKILL.md:91-92: "look at the screenshots for the step where the run diverged ... the PNGs are what the page looked like before each decision". Since f693ebb the default is observation.screenshots = "key" (spec.py:61): only the terminal step and steps flagged never_violated / low_confidence / stale / repeat_count >= 2 have a PNG, so the divergence step usually has none, and the picture is taken after Jev's answer (run_test.py:331-337). references/trace-format.md:9-14 and verdict-rubric.md:52-54 were updated; SKILL.md was not. Fix: reword to match trace-format.md ("the terminal and flagged steps have a PNG of the page Jev decided on; for the step before a divergence use --step N, or rerun with --screenshots all").

### [should_fix] README Layout block and exit-code list describe an older tree  (README.md; reported 2x)
Layout says 'scripts/selftest.py  offline: fake Jev, local pages, 8 scenarios' but selftest.py at HEAD runs 17 numbered checks (0, 0b, 1, 1b, 2-14: observer, settle, three screenshot modes, stale/unstable page, viewport-first text, CDP attach, dotenv). The block also omits scripts/rules.py, scripts/bench.py and scripts/unit_tests.py, all present at HEAD and referenced elsewhere in the README. The sentence 'Exit code 0 = passed, 1 = failed (never_violated, stuck, blocked, low_confidence, done_unverified, budget_exhausted)' omits unstable_page (added in 316c17a) and error. Fix: refresh the Layout list (add the three scripts, drop the scenario count or make it '17 checks') and add unstable_page/error to the exit-1 list.

### [should_fix] README layout says selftest has '8 scenarios' and omits new scripts  (README.md; reported 2x)
Line 106: "scripts/selftest.py offline: fake Jev, local pages, 8 scenarios". selftest.main at HEAD runs 17 numbered checks (0, 0b, 1, 1b, 2-14: observer, settle, screenshot modes, validation retry, stale/unstable, viewport-first text, CDP, dotenv). The Layout block also has no rows for scripts/bench.py, scripts/rules.py, scripts/unit_tests.py or docs/superpowers/measurements/. Update the count (or drop it) and add the rows. SKILL.md's Files section (lines ~150-155) has the same omissions.

### [should_fix] SKILL.md still describes a screenshot per step taken before the decision  (SKILL.md; reported 2x)
SKILL.md step 3 'Read the trace': 'the PNGs are what the page looked like before each decision'. At HEAD (spec 4.10, commit f693ebb) the default observation.screenshots is "key": a PNG exists only for the terminal step and steps flagged never_violated / low_confidence / stale / repeat_count >= 2, and it is captured after Jev's answer, before the action (run_test.capture, trace-format.md and spec-format.md say so correctly). A Claude following SKILL.md will look for steps/NNN.png on ordinary steps and not find it. Fix: 'the PNGs show the page Jev decided on (taken after its answer, before the action); with the default "key" policy only the terminal and flagged steps have one, rerun with --screenshots all for the rest'.

### [should_fix] A/B measurement files cite env knobs and a commit that cannot reproduce them; README implies they can  (README.md; reported 1x)
The README says each JSON holds 'the git commit and the JEV_* environment the runs were made with'. The nine 2026-09-22-track1-ab-*.json files record git_commit f693ebb and env knobs JEV_STATE / JEV_CRITERIA / JEV_RULES / JEV_CHECK_RULE / JEV_RULES_TEXT / JEV_TARGET_PHRASING, but `grep -rn JEV_` over the repo and history finds no commit that reads them (only bench.py's env capture), and the two 'nolabels' files carry an env identical to their twins (speed-only vs speed-only-nolabels: both {legacy, legacy, off, off}); the difference was an uncommitted observer edit. So no A/B row can be reproduced from any commit. Also the baseline row says 'Baseline = commit e20f461' while 2026-09-21-track1-baseline.json records git_commit 9cce64a (the tree the runs used, before bench.py was committed). Fix: add one paragraph saying the A/B variants ran on an uncommitted working tree at f693ebb behind temporary env knobs that were removed before c3382c2 (as the brief required), that 'nolabels' is the observer change committed in c3382c2, and that the adopted shape is what c3382c2/2026-09-21-track1-after.json measure; cite 9cce64a (or say 'the tree before e20f461') for the baseline. bench.py's `k.st

### [should_fix] A cdp_url nobody listens on crashes the runner with a traceback, exit code 1 and no trace.json  (run_test.py; reported 1x)
`browser = p.chromium.connect_over_cdp(cdp_url)` (line 371) runs before the `try:` at line 393, and `main()` (lines 669-678) only catches ValueError/JSONDecodeError/OSError from spec loading; `run()` at line 693 is unguarded. The most common user error for this feature (Chrome not started, wrong port, `--remote-debugging-port` forgotten) therefore surfaces as `playwright._impl._errors.Error: BrowserType.connect_over_cdp: connect ECONNREFUSED 127.0.0.1:NNNN` with a full Python traceback, exit status 1, which the docstring defines as "did not pass (see trace status)" although no trace.json exists (bench.py then records status=error from the last 500 chars of stderr). Verified with scratchpad/cdp_probe_a_refused.py: `run()` raises after 0.29 s, no trace.json; `main()` with the JevClient swapped for FakeJev (no network) propagates the same exception. The exit-code contract says 2 = spec/environment problem, and a browser that is not running is an environment problem. Fix: wrap the browser acquisition (both `connect_over_cdp` and `launch`) in `try/except Exception as e` inside `run()` and either (a) raise a dedicated `RuntimeError(f"could not attach to {cdp_url}: {e}")` that `main()` ca

### [should_fix] A pass-2 label (hidden checkbox) is fingerprinted as the label, so its control being checked or disabled during the decision is not stale  (observe.py; reported 1x)
State: an offscreen checkbox offered through its <label> (observe.py pass 2, lines 182-191, e.g. Bootstrap custom-control). describe() reports role=checkbox, checked/disabled from the control, but nodeTuple(label) reads label.checked (undefined -> null) and label.disabled (undefined -> false). Confirmed with scratchpad/fresh/probe_label.py on CONTROLS_PAGE: tuple is [True, True, '', None, False, hash]; after `#terms.disabled = true` compare_fingerprint(..., 'CLICK', idx) -> None; after `#terms.checked = true` -> None. Wrong result: Jev decided to check an unchecked box; the click on the label now UNCHECKS it (or does nothing on a disabled control) and the trace says ok=True. Fix: in JS_HELPERS make nodeTuple resolve `const c = (el.tagName === 'LABEL' && el.control) ? el.control : el;` and compute valueOf/checkedOf/disabledOf on c (keep visibleOf/scopeOf on el). Both observe and fingerprint share the helper, so no other change is needed.

### [should_fix] Acceptance line hides the badpw confidence drop the cited JSON shows  (README.md; reported 1x)
Line 24-25 states 'median decision confidence not lower ✓ (smoke-login 0.95 vs 0.94)'. Spec 4.11's criterion covers both smoke specs, and the two files the table cites show smoke-login-badpw median decision_confidence 0.70 (baseline.json) -> 0.405 (after.json), min 0.28 -> 0.04. Per-step data pinpoints where: on the step right after the failed login the operation decision reads 0.15-0.42 in the baseline and 0.04-0.12 after (after.json step 4 of every run: 0.08, 0.05, 0.04, 0.09, 0.12). The A/B files isolate it to the structured state, not the rules: speed-only-nolabels step 4 = 0.34/0.27 vs structured-no-rules-nolabels step 4 = 0.05/0.06/0.07 on the same page. This is the exact 'structured state changes Jev's behaviour -> revert per item if worse' risk from spec section 7 and the write-up does not mention it. Fix: mark the confidence criterion ✗ for smoke-login-badpw with the 0.70 -> 0.41 numbers, add the step-4 comparison, and state the decision (kept anyway for page_changed/current_value, as the last paragraph already argues) so the reader sees the trade-off that was actually made.

### [should_fix] Acceptance line marks 'median decision confidence not lower' as met while badpw dropped 0.70 -> 0.405  (README.md; reported 1x)
Line 25: 'median decision confidence not lower ✓ (smoke-login 0.95 vs 0.94)'. In the cited files smoke-login-badpw's medians.decision_confidence is 0.70 in 2026-09-21-track1-baseline.json and 0.405 in 2026-09-21-track1-after.json (per-run 0.505/0.39/0.44/0.405/0.39; min 0.04), and the headline table has no confidence row for badpw at all although it has one for smoke-login. Spec 4.11 states the criterion for both smoke specs. Fix: add the badpw confidence row, mark the criterion ✗ for badpw (or explain that the three refused low-confidence WAIT steps after the never check missed 0.8 drag the median, with the A/B batches at 0.68-0.69 as the comparison), so the write-up matches the JSON it cites.

### [should_fix] Acceptance line reports 'median decision confidence not lower' using only smoke-login; badpw dropped 0.70 -> 0.405  (README.md; reported 1x)
Spec section 4.11 asks for the before/after of both smoke specs. The README marks 'median decision confidence not lower' with a tick citing only smoke-login (0.95 vs 0.94). Recomputed from the JSON: smoke-login-badpw median decision confidence went 0.70 (baseline, values 0.63/0.72/0.64/0.72/0.70) -> 0.405 (after, values 0.505/0.39/0.44/0.405/0.39) and min_decision_confidence 0.28 -> 0.04. The Track 1 table also has no confidence row for badpw. State the badpw numbers and explain them (three refused post-login steps at 0.04-0.4 where the never check straddles 0.8), so the acceptance is not read as met on both specs. Nit in the same section: 'With the final configuration the same spec ended never_violated in 5 of 6 runs in the two A/B batches (...nolabels, ...phrasing-implicit)' - phrasing-implicit is not the final configuration (implicit target premise); say 'the adopted shape and its phrasing twin'.

### [should_fix] assets/spec.example.json still lowers max_elements to 70  (spec.example.json; reported 1x)
The example spec (SKILL.md line 184 presents it as 'a realistic spec' to copy) sets `"observation": { "max_elements": 70, "screenshots": true }`. It was written against the old default of 60 (commit 0c45b57), where 70 was a small raise; after this commit it silently caps the table at 70, below the new default 200, reintroducing the table-row truncation the commit message and spec.py comment cite as the reason for the change. Nothing in the file or SKILL.md says why an override would be wanted. Drop the `max_elements` override from the example (or set it to a value that illustrates the 1..250 range with a comment in SKILL.md).

### [should_fix] Attached mode leaves popups opened by the flow in the user's browser (launch mode closes them with the context)  (run_test.py; reported 1x)
The `finally` at lines 625-633 closes only `page`. Any tab the flow itself opened from that page (a `target=_blank` link clicked in setup or by Jev, `window.open` from the app) stays open in the user's Chrome after the run, and accumulates across `bench.py --repeat N`. In launch mode `context.close()` removes them. Verified with scratchpad/cdp_probe_d_popup.py: a spec whose setup clicks `<a target="_blank">` against an attached Chromium started with one `about:blank` tab ends with two tabs (`shop.html` + `about:blank`), i.e. LEFTOVER TABS: 1. The spec's letter ("close only that page") is satisfied, but the doc's promise "your other tabs are untouched" is about the user's tabs, not tabs the test created, so this is a behavioural gap between the two modes rather than intended. Fix: after `page = context.new_page()` register `page.on("popup", track)` where `track` appends the popup to a list and recursively registers itself on it, and in the attached branch of `finally` close every tracked popup (each in its own try/except) before `page.close()`. Do not use `context.on("page")` on the default context: it would also catch tabs the user opens by hand during the run.

### [should_fix] budget_exhausted at max_steps: the terminal step gets no picture in "key" mode  (run_test.py; reported 1x)
What is wrong: spec 4.10 says the terminal step always gets steps/NNN.png (the page Jev decided on). Every terminal break site goes through finish(), but a run that ends because the for-loop runs out (for ... else: status = "budget_exhausted", line 589-590) never calls finish(), so the last allowed step is captured only via the ordinary `capture(page, step)` at line 577, which in "key" mode skips an unflagged step. The run ends with only final.png, which shows the page AFTER the last action, so the reader has no picture of what Jev was looking at when it kept acting - precisely the status where that picture matters most.  How verified: probe in scratchpad/probe_screenshots.py, scenario 1: base_spec with budget.max_steps=2, spec default "key", FakeJev -> status budget_exhausted, step screenshots [(1, None), (2, None)], steps/ contains only final.png, summary hint reads "screenshots: no step + final.png".  Fix (one line, verified on a scratch copy scratchpad/run_test_fixed.py): at line 577 replace `capture(page, step)` with `capture(page, step, terminal=(n == budget["max_steps"]))`. With it the same probe gives [(1, None), (2, 'steps/002.png')]; a clean 5-step key run is unchanged ([

### [must_fix] cdp_check selftest is timing-fragile: snapshots the tab list before Chromium has opened its about:blank tab (fails ~10% of runs)  (selftest.py; reported 1x)
Lines 204-212 take `before = pages()` from the FIRST /json/list response that succeeds. Chromium's DevTools HTTP server starts answering a few ms before the initial `about:blank` target exists, so that first response is sometimes `[]`. After the run `after == [about:blank]`, and line 227 (`len(after) != len(before)`) reports "cdp: the browser's own tabs changed: before=[] after=['about:blank']" although the runner did nothing wrong. Verified by calling `selftest.cdp_check(url, tmp)` 20 times in a loop (scratchpad/cdp_probe_f_fixture.py): 2/20 failed with exactly that message; an independent probe (cdp_probe_b_race.py) saw the first /json/list response contain 0 pages in 2/12 launches, with the tab present 300 ms later. This makes `python scripts/selftest.py` red on a clean tree roughly one run in ten, which violates the brief's "selftest must be green after every task". Fix: treat readiness as "the endpoint answers AND lists at least one page", e.g. `before = pages(); if before: break` inside the retry loop (Chromium was started with `about:blank`, so a page is guaranteed to appear), and keep the existing `if before is None` / empty guard for the give-up message. Optionally also ch

### [should_fix] contenteditable fields always report current_value '' even when they hold text  (observe.py; reported 1x)
observe.py:152-158 computes `value` only for input/textarea/select, so a contenteditable (role 'textbox', a TYPE_TEXT target) has no value and target_criterion (policy.py:106-107) emits `current_value: ''` unconditionally for TYPE_ROLES. The commit's docstring says the empty string 'tells Jev the field is empty, which the rule about not re-filling a field needs' - for contenteditable that statement is false. Verified with scratchpad/probe_criteria2.py: `<div contenteditable aria-label='Editor'>already typed here</div>` -> {text:'already typed here', current_value:''}; after page.fill('fresh content') -> {text:'fresh content', current_value:''}. state.elements[].value is '' as well. Fix: observe.py, in the value block add `else if (el.isContentEditable) value = clean(el.innerText).slice(0, 40);` (the reference snapshot.js does `e.isContentEditable ? e.innerText.trim()`), and drop `text` for TYPE_ROLES per the previous finding so there is one value field.

### [should_fix] current_value on click targets carries a button caption for <input type=submit|button>  (policy.py; reported 1x)
Spec 4.3 / runner-design.md present `current_value` as the field's current contents, there so the TARGET rule (rules.py:41, "Do not choose a field that already contains the requested value") is decidable. At HEAD `target_criterion` (policy.py:106) adds `current_value` for ANY role whenever `e.get("value")` is non-empty, and observe.py:152-154 collects `el.value` for every <input> that is not checkbox/radio, including submit/button/reset/image. Verified with an offline Playwright probe on a local page (scratchpad/probe43): `<input type="submit" value="Login">` -> `{"element": "[4] submit \"Login\"", "role": "submit", "current_value": "Login"}` and `<input type="button" value="Go">` -> `{"element": "[5] button \"\"", "role": "button", "current_value": "Go"}` (the caption ends up only in current_value because the observer's name rule at observe.py:138 uses el.value for type=submit only). A `<button>` element is unaffected. So a click option can tell Jev that a submit button 'already contains Login', which is the opposite of what the field was added for, and it is not the documented shape. Fix: in `target_criterion` limit the value fallback to non-button roles (e.g. `e["role"] not in {

### [should_fix] Docs claim every rules wording lowered smoke-login confidence; rules-noblocked measured 0.97  (runner-design.md; reported 1x)
runner-design.md line 154-156: 'every wording tried (full, light, without the BLOCKED sentences, only the two guard sentences) lowered the operation decision's confidence'; scripts/rules.py docstring: 'with the rules attached in any form tried (...) the operation decision lost confidence (smoke-login 0.95 -> 0.50-0.57'. The cited 2026-09-22-track1-ab-rules-noblocked.json has smoke-login passed 3/3 with decision_confidence 0.97/0.97/0.97 (median 0.97, above the 0.95 no-rules variant). What that variant lost was elsewhere: badpw low_confidence 2/3 + budget_exhausted 1/3, and smoke-login took 5 actions / 6 requests / 11.3k tokens (the label no-op clicks). The 0.50-0.57 range holds for full, light and minimal only. Fix: reword both places (and the commit message is already history) to say three of the four wordings lowered confidence and the fourth traded confidence for a broken badpw outcome and extra steps, so the 'why' matches the per-variant numbers the docs point at.

### [should_fix] Docs for exit codes, STOP reasons and flags do not mention unstable_page / STALE  (trace-format.md; reported 1x)
The commit under review added the terminal status unstable_page (run_test.py:37, ended with a STOP at run_test.py:535) and the STALE summary flag (summarize_trace.py:55-56), but at HEAD: README.md:64-65 lists the exit-code-1 statuses without unstable_page; references/trace-format.md:80-81 says 'STOP means the runner stopped for its own reason (never_violated, stuck, low_confidence)' and omits unstable_page; references/verdict-rubric.md:48-50 enumerates the flags column without STALE; SKILL.md:20 lists the outcomes that come back to Claude without unstable_page, and the troubleshooting table (SKILL.md:159-175) has no row for it. Verified by grep at HEAD. Fix: add unstable_page to the README exit-code list, the trace-format STOP sentence and SKILL.md's outcome list; add STALE to the rubric's flag list; add a troubleshooting row ('unstable_page: raise browser.quiet_ms/settle_ms or add a setup wait_for; each stale step's stale field says what moved').

### [should_fix] Documented invariant is false: fingerprint text head is not a prefix of visible_text, so signature() and text_head do not hash "the same 500 chars"  (observe.py; reported 1x)
observe.py:56 says "visibleText(500) is always a prefix of visibleText(4000)"; observe() docstring (line 258), references/runner-design.md:107-108 ("the first 500 chars of the same text ... signature() hashes the same 500 chars") and the 1c3aef5 commit message repeat it. It is not true. The walker exits early on `inLen < maxChars` (line 64), so `rest` depends on maxChars; when whitespace collapsing makes the cleaned head shorter than maxChars (line 78 fails), the partial `rest` is appended (line 79). Verified with a Playwright probe (scratchpad/probe/prefix_probe.py: indented <li> text nodes plus one offscreen block first in DOM order, which is the normal state after any SCROLL_DOWN): text_head == visible_text[:500] -> False; the offscreen text appears at position 476 of text_head and at 1232 of visible_text. No runtime defect (OBSERVE_JS and FINGERPRINT_JS both call visibleText(500), so the guard compares like with like, and signature() is only compared with itself), but the comment, docstring and runner-design.md are wrong, and OBSERVE_JS walks the text tree twice (lines 231-232). Fix: walk once, build one string `text = clean(inView.join(' ') + ' ' + rest.join(' '))`, return `vi

### [should_fix] element_label docstring describes the pre-structured-state role  (observe.py; reported 1x)
Line 319: "One-line description of an element, used both in the state table and as a Choice option." Since 278a0b1 the state uses describe_element records and since the reviewed commit 12fdb1a the Choice options are target_criterion objects. At HEAD element_label is used only for trace labels (policy.resolve_target) and, via render_table, for the page signature. The commit message of 12fdb1a states this but the docstring was left as is. Rewrite the docstring accordingly.

### [should_fix] element_label docstring still describes the rendered state table and string Choice options  (observe.py; reported 1x)
observe.py:319: 'One-line description of an element, used both in the state table and as a Choice option.' Since 278a0b1 the state carries `elements` as records (policy.describe_element) and since 12fdb1a target options are objects (policy.target_criterion); grep shows element_label is now used only by render_table (which feeds signature()) and by policy.resolve_target for the human-readable `target.label` written to the trace and to recent_actions. Fix: reword to e.g. 'One-line description of an element: the `label` recorded for a chosen target in the trace and recent_actions, and the row format render_table hashes into the page signature.'

### [should_fix] Employer-domain example flow and internal-app measurements remain in fixtures and docs  (spec.example.json; reported 1x)
grep -ri the employer is clean and no company URL or handle appears, but the brief's non-negotiable is 'no employer flows; generic demo specs only'. assets/spec.example.json is an 'orders-create-pickup' spec (pickup order for 'Nile Bakery', container type 'Cardboard 660L'); SKILL.md:34 uses the same flow as the motivating example; references/runner-design.md:120 quotes a real-looking container row '1000-05-1100L Residual 1100L Nile Bakery'; references/spec-format.md:114-117 describes measurements on an internal app (an 'Impersonate' account menu, 'the app's sidebar is 17 permanent menuitems'); scripts/selftest.py:108-110 and scripts/unit_tests.py:519 reuse the same waste-container rows ('Provas2 Depot'). All of it predates the reviewed commit (introduced in 0c45b57 and 6eaaaf8) and is anonymized, so I rate it should_fix rather than must_fix; the parent may escalate. Fix: rewrite the example spec and the SKILL/runner-design/spec-format examples around the-internet.herokuapp.com pages (checkboxes, dropdown, login) and give the selftest fixture neutral row text.

### [should_fix] Employer-domain flow vocabulary survives in fixtures and docs (no name or URL, so not forbidden content)  (spec.example.json; reported 1x)
grep -ri the employer: none; no company URLs (only the-internet.herokuapp.com, typesafe.ai, example.com, 127.0.0.1). But the example spec is a waste-container pickup-order flow: "create a new pickup order for the customer 'Nile Bakery', choose the 'Cardboard 660L' container type" (assets/spec.example.json:4,7-8,20; SKILL.md:34), container IDs and types "1000-05-1100L Residual 1100L Nile Bakery" (references/runner-design.md:120; scripts/selftest.py:108-110 also "Provas2 Depot"; scripts/unit_tests.py:519,534), and measured anecdotes about an admin popup with 'Impersonate' and a 17-menuitem sidebar (references/spec-format.md:114-117). Brief section 4 and the repo rule exclude employer flows, not only names and URLs. Pre-existing since 0c45b57, not introduced by 1c3aef5. Fix: reword the example and fixtures to a generic domain (e-commerce order, generic admin menu) and keep the measured lessons without the product's nouns.

### [should_fix] Employer-domain remnants in fixtures and docs (no company name or URL)  (spec.example.json; reported 1x)
grep -ri the employer is clean and no company URL/handle is committed. What remains is domain flavour from the internal app: assets/spec.example.json (create a pickup order for 'Nile Bakery', container type 'Cardboard 660L'), scripts/selftest.py CONTROLS_PAGE rows ('1000-04-660L Cardboard 660L Provas2 Depot', '1000-05-1100L Residual 1100L Nile Bakery'), SKILL.md line 34 ('create a pickup order for Nile Bakery and save it'), references/runner-design.md line 120, scripts/unit_tests.py line 519 ('1000-05 Residual Nile'), and references/spec-format.md lines 113-117, an anecdote measured on the internal app's 'Impersonate' account menu with its 17 permanent sidebar menuitems (the user's own note says impersonation flows were scrubbed from specs/). None of it identifies the employer, and all of it predates the commit under review, so I did not rate it must_fix; it is the user's call. If it should go, swap the waste-container rows and the pickup-order example for neutral e-commerce wording consistent with the Mini Shop fixture, and reword the spec-format anecdote generically ('a sidebar with many permanent menuitems').

### [should_fix] 'Every rules wording lowered confidence to 0.50-0.57' is contradicted by the rules-noblocked A/B file  (rules.py; reported 1x)
The module docstring says that with the rules attached 'in any form tried (full, light, without the BLOCKED sentences, or just the two guard sentences) the operation decision lost confidence (smoke-login 0.95 -> 0.50-0.57 ...)'; references/runner-design.md 'Rules' repeats it ('every wording tried ... lowered the operation decision's confidence'). Recomputed from docs/superpowers/measurements/2026-09-22-track1-ab-rules-noblocked.json: smoke-login passed 3/3 with median decision confidence 0.97 (values 0.97/0.97/0.97), i.e. HIGHER than the 0.95 without rules. What went wrong for that variant was smoke-login-badpw (low_confidence 2/3, budget_exhausted 1/3 vs never_violated 3/3 for structured-no-rules) and two wasted label clicks (5 actions instead of 3). The other three rules variants do fit the range (light 0.57, full-no-check-rule 0.505, minimal 0.56). Fix: reword both places to 'three of the four wordings lowered smoke-login confidence to 0.50-0.57; the fourth (no BLOCKED sentences) kept it at 0.97 but broke the bad-password spec (low_confidence 2/3, budget_exhausted 1/3)'. The decision itself (rules off by default) stays supported.

### [should_fix] Example spec and docs use a waste-container pickup-order flow from the employer domain  (spec.example.json; reported 1x)
No company name, URL or handle appears at HEAD (grep -ri the employer is empty), but the brief's non-negotiable is "No employer-specific content in this repo (URLs, flows, company names, account handles). Generic demo specs only", and the user's memory rule says no the employer specs, URLs or flows in the public repo. assets/spec.example.json is a waste-management flow: "create a new pickup order for the customer 'Nile Bakery', choose the 'Cardboard 660L' container type", with data keys customer_name / container_type; SKILL.md line 34 repeats "create a pickup order for Nile Bakery and save it"; references/runner-design.md line 120 quotes a real-looking container row `checkbox "" in "1000-05-1100L Residual 1100L Nile Bakery"`. These predate the commit under review (0c45b57) but are present at HEAD. Fix: replace with a generic flow (the shop example already used in spec-format.md, or a the-internet.herokuapp.com flow) in the example spec, SKILL.md and the runner-design example row.

### [should_fix] Example spec pins max_elements to 70, which now lowers the cap the commit raised  (spec.example.json; reported 1x)
assets/spec.example.json:31 `"observation": { "max_elements": 70, "screenshots": true }` dates from 0c45b57 when the default was 60 (it raised it). With the 200 default from 1c3aef5 the file SKILL.md:184 calls "a realistic spec" silently truncates the table to 70 and re-creates the table-row `stuck` the commit message says truncation caused. Same file, setup step 4 uses `{"action": "wait", "ms": 1500}` while spec-format.md:97 says prefer wait_for. Fix: drop the max_elements override (or set it only when illustrating a Choice-too-large fix), and replace the wait with a wait_for.

### [should_fix] Example spec, selftest fixture and doc quotes use an employer-domain flow  (spec.example.json; reported 1x)
git grep -i the employer is clean and no company URL or handle appears anywhere tracked, but the repo's only non-demo example is recognisably a waste-management product flow: assets/spec.example.json is 'create a new pickup order for the customer Nile Bakery, choose the Cardboard 660L container type' against ${APP_BASE_URL}/orders; scripts/selftest.py:106-111 (CONTROLS_PAGE) reproduces a container table ('1000-04-660L | Cardboard 660L | Provas2 Depot', '1000-05-1100L | Residual 1100L | Nile Bakery'); references/runner-design.md:120 quotes that row; SKILL.md:34 uses 'create a pickup order for Nile Bakery and save it' as the model goal; references/spec-format.md:113-117 reports measured behaviour of 'the app's sidebar is 17 permanent menuitems ... Impersonate and Logout'. All of this predates 278a0b1 (from 0c45b57 / 6eaaaf8) and I cannot confirm the customer/depot names are real, so not must_fix; but the brief's 'no employer-specific content (URLs, flows, company names)' and the standing rule that this public repo stays generic argue for neutral fixtures. Fix: switch the example and fixture to a neutral domain (e.g. an orders/e-commerce app with 'Acme Ltd', 'Widget', quantity 2), keep t

### [should_fix] Failed-action and repeat_count>=2 captures have no selftest or unit case  (selftest.py; reported 1x)
Brief section 4 (non-negotiables): 'Every behavioural change lands with a selftest or unit case.' The selftest covers True / "key" / False on clean, never_violated, low_confidence and stale runs, but nothing exercises (a) the new `NNN-failed.png` / `step.screenshot_after_failure` capture after `executed.ok == false` (run_test.py lines 580-581), (b) the non-terminal `repeat_count >= 2` branch of `key_step`, or (c) the `stuck` terminal capture; grep for `failed.png|screenshot_after_failure|stuck|repeat_count` in selftest.py returns nothing. The CLI mapping in main() (`--screenshots` / `--no-screenshots` -> True/"key"/False) is also untested. I verified the behaviour itself with a scratchpad probe (/private/tmp/claude-501/-Users-fares-Downloads-jev-browser-test/32bfd8fe-3009-488b-b4bd-6f963d7d0254/scratchpad/probe_screenshots.py): a FakeJev that answers an unoffered click target once makes step 1 fail with 'no target answer' and yields `steps/001-failed.png` in key and True modes and nothing in False; an always-WAIT FakeJev yields repeat_count 1 (no picture), 2 (`002.png`), 3 = stuck (`003.png`) plus final.png in key mode; budget_exhausted still writes final.png. So the code is correc

### [should_fix] Fingerprint text_head is now geometry-dependent: a pure CSS animation crossing the fold makes DONE/BLOCKED/PRESS_ENTER stale with zero DOM mutations  (observe.py; reported 1x)
What is wrong: FINGERPRINT_JS (line 246) and the observation's `fingerprint.text_head` (line 232) use `visibleText(500)`, which since this commit orders text by viewport intersection (`r.top < innerHeight` etc., lines 71-75). Which nodes intersect the viewport is geometry. Spec 4.6 and brief item 7 require the fingerprint to compare 'identity and meaning, not geometry: animations move boxes without changing meaning'. Before the commit the head was `clean(body.innerText).slice(0,500)`, which is layout-order and scroll-independent.  Failing input/state: a page whose first body child is a `position:fixed` text container (toast/ticker/cookie-bar pattern) animated by CSS keyframes between top:770px and top:830px in an 800px viewport (probe: /private/tmp/claude-501/-Users-fares-Downloads-jev-browser-test/32bfd8fe-3009-488b-b4bd-6f963d7d0254/scratchpad/probe1b_geometry.py). MutationObserver counted 0 non-jev mutations, so `settle()` (DOM-quiet based) returns immediately and cannot wait it out. Over 10 observe -> 200 ms -> fingerprint cycles, `compare_fingerprint(obs['fingerprint'], fingerprint(page), 'DONE')` returned 'visible text changed' 5/10 times; the old innerText head changed 0/10.

### [should_fix] Fixtures and docs carry the employer's product-domain vocabulary and a tenant-like name  (spec.example.json; reported 1x)
`grep -ri the employer` is clean and no company URL, account handle or company name is present (URLs found: the-internet.herokuapp.com, typesafe.ai, github.com/browser-use, example.com, 127.0.0.1). However the brief also forbids employer 'flows', and several fixtures describe a waste-management ordering flow: assets/spec.example.json ('create a new pickup order for the customer Nile Bakery, choose the Cardboard 660L container type'), scripts/selftest.py CONTROLS_PAGE rows ('1000-04-660L', 'Cardboard 660L', 'Provas2 Depot', 'Residual 1100L', 'Nile Bakery'), SKILL.md ('create a pickup order for Nile Bakery and save it'), references/runner-design.md (the same row text) and references/spec-format.md ('Impersonate' menu, 'the app's sidebar is 17 permanent menuitems', measured on 'a real app'). 'Provas2 Depot' in particular reads like an internal tenant/depot name. Not introduced by c3382c2 and not a hard violation (no names/URLs), so should_fix rather than must_fix: swap the vocabulary for the shop/e-commerce domain the selftest already uses (orders of hoodies, a 'Warehouse A' column) and neutralise 'Provas2 Depot'; keep the technical lessons (antd hidden checkbox, half-true menu checks) a

### [should_fix] Fixtures and docs carry waste-management domain vocabulary that reads as the employer's app  (selftest.py; reported 1x)
`git grep -i the employer` is clean and no company URL/handle is present, but the CONTROLS_PAGE fixture (selftest.py lines 107-110) is a container table with 'Cardboard 660L', 'Residual 1100L', container ids '1000-04-660L'/'1000-05-1100L' and 'Provas2 Depot'; assets/spec.example.json is an 'orders-create-pickup' flow with a 'Cardboard 660L' container type; references/runner-design.md line 120 quotes the same row; scripts/unit_tests.py lines 519/534 use '1000-05 Residual Nile'; references/spec-format.md lines 113-117 describe the 'Impersonate' popup and the '17 permanent menuitems' sidebar of the internal app whose impersonation specs the user had scrubbed. The brief (section 4) forbids employer-specific flows and names, and the user's note says no the employer domain examples or flows. 'Provas2 Depot' in particular looks like a real tenant/depot name; if it is, this is must_fix. Fix: rename to neutral demo data (e.g. 'Acme Bakery', 'Widget 12-pack', 'Warehouse B', ids 'SKU-1004') in selftest.py, unit_tests.py, assets/spec.example.json and runner-design.md, and reword the spec-format.md anecdote without the Impersonate/sidebar specifics. These predate 7fc950f but are still at HEAD.

### [should_fix] Four quoted medians are not derivable from the measurement JSON they cite  (README.md; reported 1x)
README.md:79-88 says the table medians come from docs/superpowers/measurements/2026-09-21-track1-{baseline,after}.json; docs/superpowers/measurements/README.md:3-4 and scripts/bench.py:12-13 say every performance number comes from committed bench output. The rows 'Jev request, warm connection' (770→307 ms; badpw 751→328) and 'browser work per action' (646→147 ms; badpw 642→146) cannot be recomputed from those files: bench.py stores only per-run sums, and jev_ms/requests gives 820→448, browser_ms/actions_executed gives 743→251. I recomputed them from the git-ignored traces under runs/bench (median of per-step latency_ms.jev for steps 2+, and median of per-step latency_ms.browser over action steps) and got exactly 770/307, 751/328, 646/147, 642/146, so the numbers are right but the citation is not reproducible from the repo. Fix: have bench.py record per-run lists of step jev/browser latencies (like step_confidences) and aggregate 'jev_warm_ms' and 'browser_per_action_ms', then either regenerate the two JSON files or reword README.md, measurements/README.md and bench.py's docstring to say those two rows are medians over the per-step latencies in the bench traces and give the recipe.

### [should_fix] Guard misses an overlay / pointer-events:none / opacity:0 on the target; execute() then force-clicks and records ok=True for a click that hit nothing  (observe.py; reported 1x)
State: observation taken, then during Jev's decision a modal overlay (position:fixed, z-index) appears over the target button that sits inside a wrapper div (the usual case), OR the button gets `pointer-events:none` (a 'busy' state), OR it fades to opacity 0. Confirmed with scratchpad/fresh/probe_guard.py and probe_guard2.py: compare_fingerprint(..., 'CLICK', idx) returns None in all three cases (the tuple has no hit-test / pointer-events / opacity field; in probe 1 the overlay was only caught by accident because the button's scope was <body>). The loop therefore executes: Playwright's click waits the full action_timeout_ms (8000 default; 1.5 s in the probe) for 'intercepts pointer events', and execute()'s fallback (run_test.py lines 187-199) retries with force=True. The forced mouse event lands on the overlay/parent, the button's onclick never fires (window.clicks stayed None), yet the step is recorded executed={action: CLICK, ok: True, forced: True} and counted in actions_executed. This is exactly the 'a toast, a re-render' scenario the commit message names, and the justification in the code comment / runner-design.md line 63 ('Playwright's actionability checks already resolve ge

### [should_fix] Guard misses checked/disabled changes on label-proxied checkboxes  (observe.py; reported 1x)
Brief item 7 / spec 4.6 require the per-node tuple to carry the node's `checked` and `disabled` state so the tuple matches what Jev saw. Pass 2 of OBSERVE_JS offers a `<label>` as the checkbox it controls (`via: "label"`) and overwrites the element record with the control's `checked`/`disabled`, but `nodeTuple(c.el)` (JS_HELPERS, ~line 52) is evaluated on the label element itself: `checkedOf(label)` is null and `disabledOf(label)` is false regardless of the control. Verified with a Playwright probe (scratchpad/probe/label_probe.py): observe a Bootstrap-style offscreen checkbox + label and a plain checkbox, toggle both via JS, then `fingerprint()`; `compare_fingerprint(..., "CLICK", plain_idx)` returns `"target [2] changed: checked"` but for the label-proxied one returns `None`, and disabling the hidden control also returns `None`. So a page that toggles or disables such a control while Jev decides is executed against anyway, and the element table said `checked: false` while the tuple said `null`. Fix: in `nodeTuple`, resolve the described control first, e.g. `const c = (el.tagName === 'LABEL' && el.control) ? el.control : el;` and compute `valueOf/checkedOf/disabledOf` on `c` (keep

### [should_fix] Internal-app anecdote ('Impersonate' menu, 17-item sidebar) in the public docs  (spec-format.md; reported 1x)
Lines 113-117 illustrate check wording with measurements from "the app": "A dropdown menu listing 'Impersonate' is visible", "the app's sidebar is 17 permanent menuitems", "a small popup ... with the entries 'Impersonate' and 'Logout'". No URL or company name, but the user's own note for this repo records that impersonation flows were scrubbed as employer-specific and that only generic content belongs here. Replace with a generic example (e.g. an account menu with 'Settings' and 'Logout' on a demo site) and keep the lesson.

### [should_fix] Label skip and trace.timing landed without a selftest or unit case  (selftest.py; reported 1x)
Brief section 4: 'Every behavioural change lands with a selftest or unit case.' The commit's two runtime behaviour changes have none. Verified for the label skip by evaluating OBSERVE_JS with the line `if (tag === 'label' && el.control && seen.has(el.control)) continue;` removed against every selftest fixture (CONTROLS_PAGE, SETTLE_PAGE, with and without ?longtext=1): the element tables are identical, and the shop PAGE contains no <label> at all, so no existing assertion can detect a regression of the rule that changed the smoke specs' step count from 6 to 4 requests. The antd label in CONTROLS_PAGE was already excluded before the change by the 'not outermost pointer' rule, which is why observer_check still passes either way. Verified for trace.timing by grep: neither selftest.py nor unit_tests.py mentions 'timing'. Fix: add to CONTROLS_PAGE a visible pair `<label for="u" style="cursor:pointer">Username</label><input id="u" type="text">` and assert in observer_check that no element with tag 'label' and name 'Username' is in the table while 'I accept the terms' (pass 2, offscreen input) still is; in one run_* case assert set(trace['timing']) == {'launch_ms','navigation_ms','setup_ms

### [should_fix] Labels of input[type=submit|button|reset] leak into current_value; type=button/reset get an empty name  (policy.py; reported 1x)
The `or e.get('value')` clause introduced at policy.py:106 adds current_value for any element with an observed value. observe.py:153-154 computes `value = String(el.value)` for every non-checkbox/radio <input>, so button-like inputs carry their caption as value. Verified with scratchpad/probe_criteria.py: `<input type=submit value='Sign in'>` -> {element:'[7] submit "Sign in"', role:'submit', current_value:'Sign in'}; `<input type=button value='Cancel'>` -> {element:'[8] button ""', role:'button', current_value:'Cancel'} (observe.py:138 only uses el.value as the name for type=submit, so the caption of button/reset appears ONLY in the wrong slot); `<input type=reset value='Reset form'>` -> {element:'[9] reset ""', current_value:'Reset form'}. With rules on, TARGET tells Jev not to choose 'a field that already contains the requested value', which now applies to a Sign in button whose current_value is 'Sign in'. Fix: in target_criterion restrict current_value to `e['role'] in TYPE_ROLES` (optionally also 'file'); in observe.py use `(tag === 'input' && ['submit','button','reset'].includes(type) ? el.value : '')` for the name and skip `value` for those types (the reference snapshot.js n

### [should_fix] Low-confidence WAIT steps discard the settle result (no step.settle)  (run_test.py; reported 1x)
At HEAD line 512 the low-confidence branch calls `settle(page, spec)` and throws the return value away, while the two sibling WAIT-executed paths record it: the stale-guard WAIT (line 541, `step["settle"] = settle(page, spec)`) and the DONE confirmation (line 562). A low-confidence step has `executed: {action: WAIT}` and `latency_ms.browser`, so per references/trace-format.md line 68 ("how the post-action wait ended") and runner-design.md ("runs after every action") a reader expects `settle` there too; without it a run that ends `low_confidence` gives no evidence whether the page ever went quiet (the exact question runner-design tells the reader to answer from `cap` endings). Verified by reading HEAD and by the selftest trace, where only CLICK steps are asserted to carry `settle`. Fix: `step["settle"] = settle(page, spec)` on line 512, and extend the selftest assertion (scripts/selftest.py line 483) to every step whose `executed.action` is WAIT.

### [should_fix] Navigation-retry and options_timeout paths of settle() have no test  (selftest.py; reported 1x)
Brief section 4: "Every behavioural change lands with a selftest or unit case." settle_check (lines 137-172) covers quiet, cap and options, but not (a) "if the evaluate throws because the document navigated, retry once on the new document" nor (b) the 200 ms `options_timeout` ending. I verified both work with a throwaway probe (scratchpad/probe/nav_probe2.py: a page that keeps mutating and navigates 150 ms later): single navigation -> first evaluate throws 'Execution context was destroyed', one retry, `{ended: quiet, ms: 360}` on the new document; two chained navigations -> both attempts throw, fallback `{ended: navigated, ms: 336}`; searchbox with no options -> `{ended: options_timeout, ms: 217}`. So the behaviour is correct at HEAD, but a regression would go unnoticed. Fix: add a `#navigate` button to SETTLE_PAGE that starts a mutation interval and sets `location.href` after ~150 ms to a fixture that mutates for 150 ms then stops; assert `ended == 'quiet'` and the new title; and one `after=("TYPE_TEXT", "searchbox")` call on a page with no options asserting `options_timeout` at 200-400 ms.

### [should_fix] No selftest or unit case for the label skip or for trace.timing  (selftest.py; reported 1x)
Brief section 4: 'Every behavioural change lands with a selftest or unit case.' (1) observe.py line 207 now drops a <label> whose control is already in the table. grep for 'timing' and '<label' in scripts/selftest.py and scripts/unit_tests.py shows no assertion for it: CONTROLS_PAGE's antd labels sit inside a cursor:pointer <tr> (never offered even before the change) and the Bootstrap label is for an offscreen input (pass 2), so observer_check passes with or without the new line; I confirmed the behaviour only with a throwaway Playwright probe (old observer: 'Username'/'Remember me'/'Disabled label' offered as clickables, HEAD: dropped, pass-2 label and label of a display:none input kept). (2) trace['timing'] (run_test.py line 644) is present in every selftest trace but nothing asserts its keys. Fix: add to CONTROLS_PAGE a <label for> pointing at a visible text input with cursor:pointer and assert no element with via=='cursor' and that name (and that the pass-2 label still appears), and in one run assert set(trace['timing']) == {'launch_ms','navigation_ms','setup_ms','steps_ms','final_ms'} with non-negative ints.

### [should_fix] No test asserts page_changed == false, the signal 4.2 exists for  (selftest.py; reported 1x)
Spec 4.2 says page_changed 'is the signal that lets Jev notice its own click did nothing' and 4.11 asks for a selftest case for page_changed. The selftest (state_shape_check, lines 397-410) and unit_tests.py only assert page_changed is True on the happy path and absent on the terminal step; grep finds no assertion of a False value anywhere. I verified the behaviour itself is correct with a throwaway probe (scratchpad/probe_page_changed_false.py: a fake Jev clicking a dead button on a local page ends `stuck`; steps 1 and 2 carry page_changed False on both the trace step and the recent_actions entry Jev saw, step 3 (STOP) has none), so this is a coverage gap, not a bug. Fix: in the existing stuck/repeat selftest case, assert that the repeated CLICK entries in jev.seen_states[-1]['recent_actions'] and the corresponding trace steps have page_changed is False.

### [should_fix] Options inside a disabled <optgroup> are offered in select_target; executing them times out  (observe.py; reported 1x)
policy.build_questions (policy.py:201-205, rewritten by this commit) filters `if not o.get('disabled')`, but observe.py:161 records `disabled: !!o.disabled`, and in Chromium option.disabled reflects only the option's own attribute (probe: `[['open-1', False, False], ['closed-1', False, True]]` for o.disabled vs o.closest('optgroup[disabled]')). Verified with scratchpad/probe_criteria.py: `<optgroup label='Closed' disabled><option>closed-1</option>` is offered as key '18:1' {element:'[18] select "Grouped"', option:'closed-1', current_value:'open-1'}; resolve_target + execute -> `TimeoutError: Locator.select_option: Timeout 1500ms exceeded ... waiting for element to be visible and enabled` (8 s with the default action_timeout_ms), recorded as a failed action and a wasted step. The reference snapshot.js guards `!o.disabled && !o.closest('optgroup[disabled]')`. Fix: observe.py:161 `disabled: !!(o.disabled || o.closest('optgroup[disabled]'))` (same tuple is used by describe_element's `options` list, so the state and the question stay consistent). Pre-existing filter, but the select_target line was rewritten here and the gap kept.

### [should_fix] Options wait is satisfied by any [role=option] already visible on the page, so it ends before the suggestions for the text just typed exist  (run_test.py; reported 1x)
SETTLE_JS optionVisible() (lines 73-80, used at line 87) scans document.querySelectorAll('[role="option"]') for any visible option; it does not distinguish options that were already there from ones produced by the TYPE_TEXT. Confirmed with two probes (scratchpad settle-probe/probe_edges.py 'persistent' case and probe_retype.py): (a) a page with an always-visible listbox elsewhere (sidebar filter) plus a combobox whose suggestion arrives 150 ms after input: settle(after=('TYPE_TEXT','combobox')) returned {'ended': 'options', 'ms': 52} and the suggestion was NOT present when Jev would be asked. (b) The common re-query case: type 'blue' -> options ['Blue Hoodie','Blue Cap'] (correct, 'options' at 269 ms); then type 'cap' with the widget refreshing asynchronously (150 ms): settle returned 'options' at 106 ms with the stale ['Blue Hoodie','Blue Cap'] still shown; the real list ['Blue Cap'] arrived 300 ms later. Jev is then asked to pick from options that are about to disappear (the guard later flags 'connected' changed and a Jev request is wasted, or the click lands on the wrong item if the refresh comes after the guard). This defeats the stated purpose ('so a prediction is not paid for

### [should_fix] page_changed is false for actions whose effect lands after the DOM-quiet settle, and Jev is told the click did nothing  (run_test.py; reported 1x)
What is wrong: `page_changed` compares the decision signature with the observation taken right after `settle()`. settle() ends on DOM quiet (quiet_ms after the last mutation, 2 frames), so a click that triggers no immediate DOM mutation (fetch/XHR, setTimeout, animation-gated update) ends the settle after ~quiet_ms and the observation precedes the effect. The entry is then permanently recorded `page_changed: false` (note_page_change resolves it once and clears pending_change), Jev sees it in recent_actions (and the opt-in NEXT_ACTION rule tells it the action 'did nothing visible; do not simply repeat it'), and the trace step carries the same flag for Claude's triage. How verified: ran selftest.base_spec on the repo's own `?slow=1` fixture (cart paints 250 ms after the click; settle_ms 300 / quiet_ms 50) with the normal FakeJev: steps 4 and 5 are both `CLICK [3] button "Add to cart"` with settle `{ended: 'quiet', ms: 61/66}` and `page_changed: False`, the run ends `stuck` and `final.checks.cart_has_item = 0.02`, while the cart did update (two items were added). The same happens on any real app whose click round-trips to a server slower than quiet_ms. The smoke specs are not affected

### [must_fix] Per-request and per-action latency rows are not reproducible from the cited bench JSON  (README.md; reported 1x)
README.md ('Measured' table, rows 'Jev request, warm connection 770 -> 307 ms' and 'browser work per action (execute + settle) 646 -> 147 ms', plus the prose '2.0 s initial page load, 0.15 s browser launch (trace.timing)'), docs/superpowers/measurements/README.md (same two rows for both specs: 770/307, 646/147, 751/328, 642/146) and SKILL.md ('~0.3 s per decision on the warm connection (~0.8 s for the first request)', '~0.15 s per action', '~2 s page load') all cite docs/superpowers/measurements/2026-09-21-track1-{baseline,after}.json, and measurements/README.md states 'Every performance number in the README, SKILL.md and references/ comes from scripts/bench.py output committed here' (the brief's section 4 non-negotiable says the same). Verified by opening both JSON files: bench.py's per-run record (RUN_FIELDS in scripts/bench.py) stores only per-run sums (jev_ms, browser_ms), requests, actions_executed, tokens and step_confidences; there is no per-step latency, no warm/cold split and no trace.timing. The only statistics recomputable from the committed files give materially different values: median jev_ms/requests = 820 -> 448 ms (not 770 -> 307) and median browser_ms/actions_execu

### [should_fix] Per-request and per-action medians are not reproducible from the committed bench JSON  (README.md; reported 1x)
measurements/README.md (lines 3-4) states that every performance number in README/SKILL/references comes from bench.py output committed there. The wall, tokens and confidence rows do (stored medians equal my recomputation: smoke-login 8896->5165 ms = -42%, badpw 11563->6040 = -48%, 3858->5126 tokens, 0.94->0.95). But the rows 'Jev request, warm' (770/307, 751/328), 'browser per action' (646/147, 642/146), and README/SKILL's 'first request ~0.8 s', 'page load 2.0 s', 'launch 0.15 s' are not in either JSON: bench.measure_trace records only jev_ms/browser_ms sums, requests, tokens and confidences. I could only reproduce them (exactly: 770/307, 646/147, first 832/766, nav 1979, launch 152) by reading per-step latency_ms and trace.timing from the git-ignored runs/bench/<...>/trace.json folders on this machine, which a reader of the public repo does not have. Fix: add jev_first_ms, jev_warm_ms (median latency_ms.jev over steps >= 2), browser_per_action_ms (median latency_ms.browser over executed action steps) and timing.navigation_ms/launch_ms to RUN_FIELDS/measure_trace in scripts/bench.py, backfill the two track1 JSON files from the local traces (they all still exist), or at least say 

### [should_fix] Per-request and per-action numbers are not reproducible from the committed measurement JSON  (README.md; reported 1x)
The measurements README (line 3) and scripts/bench.py's docstring (lines 12-13) say every performance number comes from bench.py --json output committed under docs/superpowers/measurements/. That holds for wall, tokens, confidence and status counts (I recomputed every median in all 12 JSON files; they all match the stored medians and the README/SKILL tables: 8,896/5,165 ms −42%, 11,563/6,040 ms −48%, 0.94/0.95, 3,858/5,126, and every A/B row). It does NOT hold for the rows 'Jev request, warm' (770/307, 751/328), 'browser per action' (646/147, 642/146), README's '2.0 s page load / 0.15 s launch' and SKILL.md's '~0.8 s first request': the JSON only carries per-run sums (jev_ms, browser_ms, requests, actions_executed), and jev_ms/requests gives 820/448 and browser_ms/actions gives 743/251, not the quoted figures. I recovered the quoted values only from the per-step latency_ms in the git-ignored runs/bench/*/trace.json (medians over steps>=2 and over executed action steps, plus trace.timing: launch 152, navigation 1979). The numbers are right, but a reader of the repo cannot verify them. Fix: have bench.py record per run `first_request_ms`, `warm_request_ms` (median latency_ms.jev over

### [should_fix] Per-request / per-action latency rows are not derivable from the cited bench JSON (provenance claim untrue)  (README.md; reported 1x)
The README opens with 'Every performance number in the README, SKILL.md and references/ comes from scripts/bench.py output committed here', but bench.measure_trace records only per-run sums (jev_ms, browser_ms), requests, actions_executed and step_confidences; no per-step latency_ms and no trace.timing. Recomputed from 2026-09-21-track1-baseline.json and 2026-09-21-track1-after.json: wall 8,896 -> 5,165 ms (-42%), badpw 11,563 -> 6,040 (-48%), confidence 0.94 -> 0.95, input tokens 3,858 -> 5,126, outcomes passed 5/5 and never_violated 5/5 -> low_confidence 5/5 all MATCH. But the rows 'Jev request, warm 770 -> 307 ms' (badpw 751 -> 328) and 'browser per action 646 -> 147 ms' (badpw 642 -> 146) cannot be recomputed from those files; the only derivable equivalents are jev_ms/requests = 820 -> 448 ms (badpw 758 -> 405) and browser_ms/actions_executed = 743 -> 251 ms (badpw 1,145 -> 323). The same applies to README.md ('Jev request, warm connection 770 ms / 307 ms', 'browser work per action 646 / 147', 'about 2.0 s is the initial page load and 0.15 s the browser launch (trace.timing)') and SKILL.md ('~0.3 s per decision ... ~0.8 s for the first request', '~0.15 s per action', '~2 s is t

### [should_fix] README cites numbers that exist in no committed bench JSON  (README.md; reported 1x)
The file opens with 'Every performance number in the README, SKILL.md and references/ comes from scripts/bench.py output committed here' (brief section 4 makes this a non-negotiable), but the rows 'Jev request, warm' (770/307 ms and 751/328 ms) and 'browser per action' (646/147 ms and 642/146 ms), the never-check readings '0.73-0.84', '0.75/0.76/0.78 for three steps, then 0.80-0.84', and (in README.md line 90 and SKILL.md lines 13-16) '~0.8 s for the first request', '0.15 s the browser launch', '2.0 s is the initial page load' cannot be derived from any file in docs/superpowers/measurements/. Verified: grep over all 12 JSONs finds no 307 and no 646 anywhere; bench.measure_trace (scripts/bench.py:43-66) records only per-run sums jev_ms/browser_ms, request counts and decision confidences, never per-step latencies, check probabilities or trace.timing. From the committed after.json the only derivable per-action figure is browser_ms/actions = 754/3 = 251 ms, not 147; and 3282/4 = 820 ms per request, not 770. Fix: extend measure_trace with jev_ms_warm (median of latency_ms.jev over steps after the first), browser_ms_per_action (median of latency_ms.browser over steps whose executed.actio

### [should_fix] README exit-code list and Layout section are stale  (README.md; reported 1x)
(a) Line 64: exit code 1 is listed as `never_violated`, `stuck`, `blocked`, `low_confidence`, `done_unverified`, `budget_exhausted`; at HEAD run_test.TERMINAL_STATUSES also has `unstable_page` (added in 316c17a) and `error`, both exit 1. (b) Line 106: "scripts/selftest.py offline: fake Jev, local pages, 8 scenarios" - selftest.py at HEAD has 17 numbered scenario blocks (0, 0b, 1, 1b, 2-14: settle, screenshot modes, validation retry, stale/unstable_page, viewport-first text, cdp_url, dotenv). (c) The Layout block omits scripts/rules.py, scripts/bench.py, scripts/unit_tests.py and docs/superpowers/measurements/, all of which are tracked and referenced elsewhere in the README. Fix: add the two statuses, drop or correct the scenario count, list the missing files.

### [should_fix] README exit-code list omits unstable_page and error  (README.md; reported 1x)
Line 64 lists the exit-1 statuses as never_violated, stuck, blocked, low_confidence, done_unverified, budget_exhausted. run_test.TERMINAL_STATUSES at HEAD also has `unstable_page` (Track 1 freshness guard) and `error`; both exit 1. Add them.

### [should_fix] README exit-code list, selftest scenario count and Layout are stale  (README.md; reported 1x)
(1) 'Exit code ... 1 = failed (never_violated, stuck, blocked, low_confidence, done_unverified, budget_exhausted)' omits `unstable_page` (and `error`, which also exits 1 via `return 0 if trace["pass"] else 1`). (2) Layout: 'scripts/selftest.py  offline: fake Jev, local pages, 8 scenarios' - HEAD's selftest.main has numbered scenarios 0, 0b, 1, 1b, 2-14 (observer, settle, cdp, dotenv, viewport-first text, stale/unstable, screenshot modes...). (3) Layout omits scripts/bench.py, scripts/rules.py, scripts/unit_tests.py and docs/superpowers/measurements/ even though the Measured section cites bench.py and the measurement files. Update the three spots.

### [should_fix] README rows 'Jev request, warm', 'browser per action' and trace.timing figures are not in the cited bench JSON  (README.md; reported 1x)
The Track 1 table cites 2026-09-21-track1-{baseline,after}.json for 'Jev request, warm 770 -> 307 ms' and 'browser per action 646 -> 147 ms' (badpw 751 -> 328, 642 -> 146), and the top-level README.md and SKILL.md repeat them plus '2.0 s page load / 0.15 s launch (trace.timing)'. The committed bench records only carry per-run sums (jev_ms, browser_ms) and counts (requests, actions_executed) - no per-step latencies and no trace.timing (scripts/bench.py measure_trace never reads it). Verified by loading every run record: the only derivable figures are total jev_ms/requests 820 -> 448 ms and browser_ms/actions_executed 743 -> 251 ms, which do not match the quoted rows, so those numbers came from uncommitted runs/ traces. Brief section 4 requires every README/docs number to come from bench.py output with the files committed. Fix: extend bench.measure_trace/aggregate with jev_warm_ms (median latency_ms.jev over steps after the first), browser_per_action_ms (median latency_ms.browser over executed actions) and the trace.timing medians, re-run the two 5x batches (or drop those rows and the timing sentence until they are recorded).

### [should_fix] README says selftest has 8 scenarios; it runs 14  (README.md; reported 1x)
README.md 'Layout' section: 'scripts/selftest.py  offline: fake Jev, local pages, 8 scenarios'. Verified with grep: scripts/selftest.py calls run() 14 times (run-pass, run-pass-key, run-error, run-blocked, run-hedge-value, run-early-low-done, run-done-race, run-done-unverified, run-invalid-op-once, run-invalid-op-always, run-stale-once, run-unstable, run-longtext, run-cdp) plus the settle, observer and dotenv checks. The count predates Track 1 (freshness guard, validation, screenshot policy and CDP scenarios were added since). Fix: update the count or drop the number ('fake Jev, local pages, one scenario per terminal status and guard').

### [should_fix] README says the selftest has 8 scenarios; it has 15  (README.md; reported 1x)
README.md:106 "scripts/selftest.py offline: fake Jev, local pages, 8 scenarios". scripts/selftest.py main() has numbered cases 0-14 (observer, happy path, never_violated, blocked, low_confidence, low-conf DONE, DONE confirmation, done_unverified, retry, error after retry, stale target, unstable_page, viewport-first text, cdp_url, .env). Fix: "15 scenarios" or drop the count.

### [should_fix] Rules narrative over-generalises: the 'noblocked' wording did not lower smoke-login confidence  (rules.py; reported 1x)
scripts/rules.py docstring: 'with the rules attached in any form tried (full, light, without the BLOCKED sentences, or just the two guard sentences) the operation decision lost confidence (smoke-login 0.95 -> 0.50-0.57 ...)'. references/runner-design.md ('Rules'): 'every wording tried (full, light, without the BLOCKED sentences, only the two guard sentences) lowered the operation decision's confidence'. Verified against docs/superpowers/measurements/2026-09-22-track1-ab-rules-noblocked.json: smoke-login median decision confidence 0.97 (per run 0.97/0.97/0.97, passed 3/3), i.e. higher than the no-rules 0.95, not in 0.50-0.57. What that variant did cost was two extra actions (5 instead of 3, first/third decisions at 0.51-0.64) and it still broke the bad-password spec (low_confidence 2/3, budget_exhausted 1/3, conf 0.44). The measurements/README.md A/B table already shows 0.97 correctly, so the two summaries contradict the table they point to. Also 'Without them the same runs pass at 0.95-0.99' omits phrasing-implicit at 0.94. Fix: reword both places to 'three of the four wordings lowered the median confidence to 0.50-0.57; the wording without the BLOCKED sentences kept 0.97 but took 

### [should_fix] runner-design Files table is stale and incomplete  (runner-design.md; reported 1x)
Line 39 describes observe.py as "The injected JS that builds the element table; `render_table`, `signature`": render_table is now only an implementation detail of signature() (nothing else calls it), while the file's other public pieces (fingerprint, compare_fingerprint, the freshness guard the same doc explains at length) are not named. The table has no rows for scripts/rules.py, scripts/bench.py or scripts/unit_tests.py although the doc has a 'Rules' section and cites bench.py measurements. Refresh the table.

### [should_fix] Runner-inserted WAITs (low confidence, stale) no longer wait: they only run the ~quiet_ms settle, unlike the Jev-chosen WAIT and the DONE recheck  (run_test.py; reported 1x)
The commit made the WAIT operation sleep settle_ms then settle (line 220) and the DONE confirmation likewise (lines 561-562), but the low-confidence branch (line 512, `settle(page, spec)`) and the stale branch (line 541) call settle() alone. Before the commit settle() was a fixed 600 ms pause (plus load), so a low-confidence step gave the page >=600 ms; at HEAD it gives about quiet_ms (the after-measurement traces show `latency_ms.browser` 105-112 ms on every low-confidence WAIT step, e.g. runs/bench/smoke-login-badpw/20260922-084345-0*/trace.json steps 4-5). Consequence: thresholds.max_low_confidence_steps (3) consecutive undecided answers on a page that is still loading (spinner, pending fetch) now end the run `low_confidence` after ~0.3 s of browser time instead of ~1.8 s, while the comment at lines 489-492 still justifies the WAIT with 'the page may still be settling', and references/trace-format.md describes low-confidence steps as WAITs. The smoke specs' outcomes are unaffected (their low-confidence steps happen on a static page), but this is an unintended behavioural change of the commit for slow-rendering apps. Also inconsistent: the stale WAIT records step['settle'] but th

### [should_fix] searchbox option-wait never arms for native <input type=search> or datalist inputs  (observe.py; reported 1x)
Spec 4.5 / brief item 6: after TYPE_TEXT into a `combobox`/`searchbox`, wait up to 200 ms for a visible `[role=option]`. run_test.py line 582 looks up the observed `role` and line 113 checks it against AUTOCOMPLETE_ROLES, but observe.py lines 122-129 derive `role` from `getAttribute('role')` and otherwise map every text-like `<input>` to `textbox`. Probe (scratchpad/probe/role_probe.py, Playwright + local HTML): `<input type="search">` -> role 'textbox' (armed: False); `<input list="dl">` -> 'textbox' (False); only explicit `role="combobox"`/`role="searchbox"` arm the wait. Native search inputs have the implicit ARIA role `searchbox` (and `input[list]` is implicitly `combobox`), so the `searchbox` half of the requirement is reachable only through an explicit attribute. Impact is limited (most autocomplete widgets do set role=combobox, as the reference browser.py also assumes), hence should_fix rather than must_fix. Fix: in observe.py's role derivation add `type === 'search' -> 'searchbox'` and `el.hasAttribute('list') -> 'combobox'` (spec 4.5 lists scripts/observe.py as a file in scope), or accept `type == 'search'` in the run_test.py target_role lookup; add the two inputs to CONTR

### [should_fix] Secrets typed into non-password fields come back unmasked via current_value / text / label  (policy.py; reported 1x)
target_criterion (policy.py:104-107) copies the observed `value` into `current_value` and `text` verbatim; describe_element does the same into state.elements[].value/label/text; run_test.py puts obs['elements'] into step['elements']. Only `input[type=password]` is masked by the observer ('(filled)') and only available_data_values/type_value go through value_preview. Verified with scratchpad/probe_secret.py: spec data {token: 'hunter2-not-real'}, secrets ['token']; execute TYPE_TEXT of `token` into `<input type=text aria-label='API token'>`, an aria-labelled contenteditable and an unlabelled contenteditable, then observe + build_state + build_questions -> the plain secret appears in questions.type_target['0'].current_value, in ['2'].text, in ['3'].element ('[3] textbox "hunter2-not-real"' because name falls back to text), in state.elements and in what becomes trace step.elements. The commit's unit test `assertNotIn('pw', json.dumps(self.questions))` only covers the data-values channel. Pre-existing in element_label (value="...") and the state; this commit adds two more unmasked channels (current_value, text) to the criteria. Fix: a Python-side mask (never pass secrets into page JS):

### [should_fix] Several quoted numbers are correct but not reproducible from the committed measurement JSON (bench.py records only per-run sums)  (README.md; reported 1x)
Recomputed from the JSON: wall 8896->5165 (-41.9%), 11563->6040 (-47.8%), tokens 3858/5126 (~1.3k per step), confidence 0.94/0.95, statuses, and every row of the per-item A/B table (all ten ab-*.json) match. But "Jev request, warm 770 / 307" and "751 / 328" (README.md:85, measurements/README.md:15,21, SKILL.md:13-14 "~0.3 s ... ~0.8 s for the first request"), "browser per action 646 / 147" and "642 / 146" (README.md:86, measurements/README.md:16,22, SKILL.md:15 "~0.15 s"), and README.md:90-91 "about 2.0 s ... page load and 0.15 s the browser launch (trace.timing)" cannot be derived from the cited files: the JSON medians give jev_ms/requests = 820 (baseline) and 448 (after), browser_ms/actions = 743 and 251, and no timing at all. I reproduced the quoted values only from the git-ignored runs/bench traces (median of 2nd+ requests 770/307, 751/328; median per executed action 646/147, 642/146; navigation_ms 1979, launch_ms 152). Brief section 4 requires every number to come from bench.py output with the files committed. Also note "browser per action 147 ms" is the per-step median; the mean per action in the same runs is 255 ms (754/3) because the navigating Login click costs ~450 ms - s

### [should_fix] shot() has no capture timeout of its own and swallows failures silently; a stuck web font blocks every terminal capture for action_timeout_ms  (run_test.py; reported 1x)
What is wrong: shot() (lines 316-324) calls page.screenshot(path=...) with no timeout, so it inherits page.set_default_timeout(action_timeout_ms) = 8000 ms, and on any exception returns None without recording why. Playwright's screenshot waits for document.fonts.ready before capturing; on a page whose @font-face request never completes (blocked CDN, corporate proxy, slow network) that wait lasts the full timeout and then throws TimeoutError. Under the new policy this hits the terminal step AND final.png on every run of such a page (>= 16 s added, no pictures, trace shows screenshot: null with no explanation), plus every flagged step. Because this commit moved the capture to between the freshness guard (line 528) and execute() (line 579), the same 8 s wait also reopens the window the guard just closed for steps that go on to execute (every step with --screenshots all; never_violated-with-fail_fast=false and repeat_count>=2 steps in "key" mode).  How verified: scratchpad/probe_cdp_and_font.py part (b): a local HTTP server that never answers /font.woff2; after goto, document.fonts.status == "loading"; page.screenshot() with a 3000 ms default timeout threw `TimeoutError: Page.screensho

### [should_fix] SKILL.md says screenshots show the page 'before each decision'  (SKILL.md; reported 1x)
Line 92: "the PNGs are what the page looked like before each decision". At HEAD (run_test.capture) the picture is taken after Jev's answer and before execution, and with the default `screenshots: "key"` only terminal/flagged steps get one. trace-format.md and verdict-rubric.md were updated for this; SKILL.md step 3 was not. Reword to: taken after Jev's answer, before the action; only key steps by default; `--screenshots all` for every step.

### [should_fix] SKILL.md still describes the old per-step screenshot behaviour  (SKILL.md; reported 1x)
Lines 91-92 (section 3 'Read the trace') say: "Then look at the screenshots for the step where the run diverged and at `steps/final.png` (use your image viewing; the PNGs are what the page looked like before each decision)." Both halves contradict the shipped 4.10 policy: the picture is now taken after Jev's answer (the page Jev decided on), and with the default `"key"` the divergence step usually has no picture at all (only the terminal step, flagged steps, `NNN-failed.png` and `final.png` exist), which is exactly the caveat the spec asked to be documented and that references/trace-format.md and references/verdict-rubric.md now carry. SKILL.md is the entry document Claude reads first, so it sends the reader looking for files that do not exist. Verified by grep: this is the only remaining 'before each decision' wording in the repo. Fix: reword to match spec-format.md line 58 / trace-format.md lines 9-15 (taken after Jev's answer, before the action; default `"key"` = terminal and flagged steps + `NNN-failed.png` + `final.png`; rerun with `--screenshots all` when a human needs an ordinary step's picture).

### [should_fix] SKILL.md still routes the 'existing login session' case to storage_state only  (SKILL.md; reported 1x)
Spec 4.9 states the purpose of cdp_url: 'This is how SSO-walled internal apps get tested without scripting login', and the brief says each track ends with doc updates. The recipe landed in references/spec-format.md and the trace field in trace-format.md, but SKILL.md (the file Claude actually reads) has no mention of cdp_url / --cdp-url: its troubleshooting row 'Page needs an existing login session' (line 174) only says 'Save a Playwright storage_state once and point browser.storage_state at it', which does not work for SSO/hardware-token logins. Fix: extend that row (or add one) to point at `browser.cdp_url` / `--cdp-url` and the 'Attach to a running browser' section of spec-format.md, noting that storage_state is ignored when attached.

### [should_fix] SKILL.md still says screenshots show the page 'before each decision'  (SKILL.md; reported 1x)
SKILL.md lines 91-92: 'the PNGs are what the page looked like before each decision'. At HEAD (run_test.capture, lines 331-337) the picture is taken after Jev's answer and before execution, and with the default `screenshots: "key"` only terminal and flagged steps have one, as spec-format.md line 58 and trace-format.md lines 9-14 correctly state. Fix: reword to 'the page Jev decided on (captured after its answer, before the action); with the default key policy only terminal and flagged steps have one, rerun with --screenshots all for every step'.

### [should_fix] Stale file inventories and selftest count in README, SKILL.md and runner-design.md  (README.md; reported 1x)
README.md line 106 says selftest.py runs '8 scenarios'; at HEAD main() runs 17 checks (0, 0b, 1, 1b, 2-14 including settle, cdp, stale, unstable, longtext, dotenv). The README Layout block (lines 98-109), SKILL.md Files section (lines 179-184) and the runner-design.md Files table (lines 36-45) omit scripts/rules.py, scripts/bench.py and scripts/unit_tests.py, and runner-design.md line 39 still describes observe.py by 'render_table, signature' although render_table is now only an internal helper of signature() and the module's other exports (observe, fingerprint, compare_fingerprint) are what the loop uses. Fix: update the three inventories and replace '8 scenarios' with a count-free phrase.

### [should_fix] stale_streak is not reset by an intervening low-confidence step  (run_test.py; reported 1x)
Spec 4.6 and the brief say `max_stale` *consecutive* stale steps end the run. In `run()` the low-confidence branch (lines 502-515) `continue`s before `stale_streak = 0` (line 545), while `low_streak = 0` (line 516) runs before the guard, so a stale step does reset the low streak but a low-confidence step does not reset the stale streak. Sequence stale -> low -> stale -> low -> stale ends as `unstable_page` although no two stale decisions were consecutive (TERMINAL_STATUSES and trace-format.md both promise 'consecutive'). Verified by reading the control flow; the selftest's ?unstable=1 fixture cannot expose it because FakeJev is never low-confidence there. Fix: add `stale_streak = 0` in the low-confidence WAIT branch (next to `low_streak += 1`), or move both resets to one place after the gates.

### [should_fix] `text` in type_target criteria is the textarea's static default content and contradicts current_value after an edit  (policy.py; reported 1x)
target_criterion emits `text` whenever the observer has one (policy.py:104-105). For a <textarea> the observer's `text` is clean(el.innerText||el.textContent), i.e. the initial markup content, not the live value. Verified with scratchpad/probe_criteria2.py: `<textarea aria-label='Notes'>hello there</textarea>` -> before edit {text:'hello there', current_value:'hello there'} (redundant); after page.fill('completely new text') -> {text:'hello there', current_value:'completely new text'}: two contradictory value-like fields on the very question whose TARGET rule says 'Do not choose a field that already contains the requested value'. For a `<select role=combobox>` `text` is the joined option list ('One Two'). The spec (4.3) and brief (item 4) list element/role/current_value/checked/context only; `text` was added by the commit. Fix: in target_criterion skip `text` for e['role'] in TYPE_ROLES (keep it for clickables such as rows), or in observe.py do not compute `text` for input/textarea/select. Smoke specs are unaffected (no textarea on the login page).

### [should_fix] Text that is a direct child of a closed <details>, a content-visibility:hidden element or hidden="until-found" leaks into visible_text (innerText excluded it)  (observe.py; reported 1x)
What is wrong: the visibility test is `parent.checkVisibility(...)` on the text node's parent (line 69) plus a non-empty Range rect (lines 70-72). For a text node whose parent IS the element that skips its contents (a closed `<details>`, `content-visibility:hidden`, `hidden="until-found"`), `checkVisibility` is true (the element itself is rendered) and Chromium 153.0.8010.12 (Playwright 1.63) returns a non-empty Range rect for the skipped text, so the text is emitted. Nested text (`<details><p>..</p></details>`) is correctly excluded because the `<p>`'s checkVisibility is false. Verified in .../scratchpad/probe6_details.py: 'DIRECT-CHILD-ANSWER', 'CV-DIRECT', 'UNTIL-FOUND direct' are in `visible_text` and NOT in `body.innerText`; rects were [320,18], [159,18], [177,18] with clientRects=1.  Failing input/state: `<details><summary>Show error</summary>Payment failed: card declined</details>` (collapsed). Wrong result: `visible_text` (the only text Noul checks judge, rules.CHECK says 'as currently shown') contains text the user cannot see, so a check like 'an error message is visible' or a `never` check can fire on collapsed content; pre-commit `body.innerText` correctly omitted it. Th

### [should_fix] The A/B measurements the opt-in decision cites are not reproducible from any commit  (README.md; reported 1x)
What is wrong: rules.py's docstring, references/runner-design.md (lines 154-159) and docs/superpowers/measurements/README.md justify `"rules": false` by pointing at `docs/superpowers/measurements/2026-09-22-track1-ab-*.json`. Those files record `git_commit: f693ebb` and `env: {JEV_STATE, JEV_CRITERIA, JEV_RULES, JEV_CHECK_RULE, JEV_RULES_TEXT}`, and the measurements README says each file holds "the git commit and the JEV_* environment the runs were made with", i.e. that they can be re-run. How verified: `grep -rn JEV_ scripts references README.md` finds only bench.py's env capture; `git log --all -S'JEV_RULES' -- scripts` and `-S'JEV_STATE'` return nothing, and `git show f693ebb:scripts/policy.py` has no os.environ read. So the knobs never existed in committed code, and the 'light', 'minimal', 'noblocked' and 'no-check-rule' rule texts are not in rules.py at HEAD (only the full NEXT_ACTION/TARGET/VALUE/CHECK). Checking out f693ebb and setting those variables reproduces the default behaviour, not the variants. Wrong result: the documented reason for shipping the rules off by default cannot be re-measured, and the `--json` output's `env` block in scripts/bench.py line 164 now capture

### [should_fix] The combobox/searchbox options wait can never fire for a native <input type=search>, aria-autocomplete=list inputs or <input list>: observe() labels them 'textbox'  (observe.py; reported 1x)
run_test.settle() gates the options wait on the observed role being in AUTOCOMPLETE_ROLES = ('combobox','searchbox') (run_test.py line 113), and the role comes from observe.py's derivation at line 129: for <input> without an explicit role attribute, any type not in ['checkbox','radio','submit','button','file','range','reset','image'] becomes 'textbox'. Confirmed by probe: <input type="search" placeholder="Search products"> -> role 'textbox'; <input aria-autocomplete="list"> -> 'textbox'; <input list="dl"> -> 'textbox'; only an explicit role="combobox"/role="searchbox" attribute reaches the new code. The native search input has implicit ARIA role searchbox (and <input list> is a combobox), and the reference snapshot.js maps e.type==='search' to 'searchbox' (line 38). So the brief-6 feature is silently a no-op for the most common search box unless the app happens to set the attribute. Fix (one line each in observe.py's role table): `else if (tag === 'input' && type === 'search') role = 'searchbox'; else if (tag === 'input' && (el.hasAttribute('list') || el.getAttribute('aria-autocomplete') === 'list')) role = 'combobox';` (or, minimally, in run_test.py derive waitForOptions from the 

### [should_fix] Two behavioural changes of c3382c2 have no selftest or unit case (label skip, trace.timing)  (selftest.py; reported 1x)
Brief section 4: 'Every behavioural change lands with a selftest or unit case.' (1) observe.py pass 3 now skips `<label>` whose control is already in the table. A scratchpad probe (local login page with `<label for>` + visible inputs, cursor:pointer) shows the line matters: at HEAD the table is [textbox Username, textbox Password, button Login]; with the line removed it gains `clickable "Username"` and `clickable "Password"` (via cursor). Nothing asserts this: CONTROLS_PAGE's only `<label for>` wraps an offscreen input (pass-2 path) and the antd labels are already excluded by the outermost-pointer rule, so the new line is never exercised. (2) `trace["timing"]` (launch_ms, navigation_ms, setup_ms, steps_ms, final_ms) has no assertion: `grep -n timing scripts/selftest.py scripts/unit_tests.py` is empty (the values are present in selftest traces, e.g. run-blocked timing={'launch_ms': 155, 'navigation_ms': 78, 'setup_ms': 0, 'steps_ms': 119, 'final_ms': 0}). Fix: add a labelled visible text input to CONTROLS_PAGE and assert no `clickable` row carries the label text; in scenario 1 assert `set(trace['timing']) == {...five keys...}` and that all values are ints >= 0.

### [should_fix] Typed secret values reach Jev and the trace through observed element values  (observe.py; reported 1x)
What is wrong: `secrets` masking covers only `available_data_values` / `type_value`. Once a secret is typed into any input that is not `type=password`, the observer reads it back as the element's `value` and it flows into (a) `state.elements[].value` (policy.describe_element), (b) `type_target` criteria `current_value` (policy.target_criterion), (c) `recent_actions[].target`, because resolve_target's label is `element_label(el)` which appends `value="..."` — that label then persists for HISTORY_WINDOW=10 steps, and (d) `trace.json` `steps[].elements[].value` and `steps[].target.label`. This contradicts references/spec-format.md ("values must never appear in the trace or in the state sent to Jev") and trace-format.md ("Typed values are never stored"). How verified: probe in scratchpad/probe_state.py cases 0 and 4 plus a follow-up run: spec with data.password="hunter2-not-real" in `secrets`, TYPE_TEXT it into `<input placeholder="Plain text">`, then TYPE_TEXT again into the same field. Result: 4 of 5 states sent to the fake Jev contain the secret, `recent_actions` entry 2 has target `[2] textbox "Plain text" value="hunter2-not-real"`, `type_target` criteria has `current_value: "hunte

### [should_fix] Unreachable cdp_url ends in an uncaught traceback, exit 1 and no trace.json  (run_test.py; reported 1x)
In run(), `browser = p.chromium.connect_over_cdp(cdp_url)` (line 371) sits before the `try:` that produces the `error` status, and main() has no `except` around run(). Verified offline with a probe (FakeJev, cdp_url pointing at a closed port): run() raises `playwright._impl._errors.Error: BrowserType.connect_over_cdp: connect ECONNREFUSED` and no trace.json is written; through the CLI (`--cdp-url http://127.0.0.1:1`, dummy key, no API call made) the process prints the raw traceback and exits 1, which the module docstring defines as 'did not pass (see trace status)' although there is no trace; an unreachable browser is a '2 = spec / environment problem' by the documented contract. Chrome not started / wrong port is the most likely user-facing failure of this feature, and the doc recipe in spec-format.md does not mention what happens. Fix: catch `playwright.sync_api.Error` around connect_over_cdp (and launch) and either return exit 2 from main() with a one-line hint ('could not reach the browser at <url>; is Chrome running with --remote-debugging-port?'), or write a trace with status `error` and `trace.error` set so the run is still exit 1 with evidence. Add a selftest/unit case with

### [should_fix] unstable_page missing from SKILL.md, README and verdict rubric  (SKILL.md; reported 1x)
The new terminal status is documented only in references/trace-format.md and spec-format.md. Places that enumerate the statuses Claude must triage do not mention it: SKILL.md line 20 ('Only the outcomes come back to you: passed, blocked, a never check firing, low confidence, a stuck loop, or a budget running out'), the SKILL.md Troubleshooting table (lines ~160-175, which has a row for every other non-pass status with its fix), README.md lines 64-65 (exit code 1 list: never_violated, stuck, blocked, low_confidence, done_unverified, budget_exhausted), and references/verdict-rubric.md line 35 (FLAKY / ENVIRONMENT signatures), even though spec 4.6 states the typical verdict ('environment: raise quiet_ms/settle_ms or add a setup wait'). Fix: add `unstable_page` to the README exit-code list and SKILL.md outcomes sentence, a troubleshooting row ('unstable_page | the page never holds still; read each step's `stale` reason, raise browser.quiet_ms/settle_ms or add a setup wait_for; verdict FLAKY/ENVIRONMENT, not BUG'), and name it in the rubric's environment paragraph.

### [should_fix] 'visibleText(500) is always a prefix of visibleText(4000)' is false: text_head can be shorter than 500 and contain off-screen text ahead of still-visible text  (observe.py; reported 1x)
What is wrong: the loop stops on `inLen < maxChars` (line 64) where `inLen` sums `value.length` of UNTRIMMED collapsed values (line 73-74), but the returned `head` is `clean(inView.join(' '))` (line 77), which collapses each value's leading/trailing space into the join separator and trims. So `inLen >= maxChars` while `head.length < maxChars` whenever in-view text nodes carry surrounding whitespace (any pretty-printed HTML: `<td>\n  Value\n</td>`). Then line 79 appends the PARTIAL `rest` collected so far (off-screen nodes that came earlier in DOM order) instead of the in-view nodes the cut-short walk never reached.  Failing input: 100 in-view spans each `\n  wNNN \n` preceded in DOM order by one node at top:5000px (probe: .../scratchpad/probe2_prefix.py). Result: `len(visibleText(500)) == 440`, `visibleText(4000).startswith(visibleText(500)) == False`; `visibleText(500)` ends '... w083 OUTVIEW-FIRST-IN-DOM' while `visibleText(4000)[:500]` continues '... w083 w084 w085 ...'; and on the same page `obs['fingerprint']['text_head'] != obs['visible_text'][:500]`.  Wrong result: the comment at observe.py lines 54-56 and references/runner-design.md lines 106-107 ('The fingerprint's text he

### [should_fix] visibleText cutoff drops in-view text for out-of-view text; text_head is not visible_text[:500]  (observe.py; reported 1x)
In JS_HELPERS.visibleText the walk stops when `inLen >= maxChars`, but `inLen` sums `raw.replace(/\s+/g, ' ').length` (leading/trailing spaces kept, one per padded text node) while `head = clean(inView.join(' '))` trims and collapses. On any page with whitespace-padded text nodes (ordinary indented HTML) inLen overshoots head.length, the walk exits before the cleaned head reaches maxChars, and the `head.length < maxChars` branch appends `rest` (the out-of-view nodes seen so far) while the not-yet-walked IN-VIEW nodes are silently lost. Two consequences: (a) the spec 4.8 requirement 'text nodes inside the viewport first, then the rest, up to max_text_chars' is violated at the boundary: below-the-fold text displaces on-screen text, exactly the crowding-out the item was meant to fix; (b) `fingerprint.text_head = visibleText(500)` is NOT a prefix of `visible_text = visibleText(4000)`, so the code comment ('visibleText(500) is always a prefix of visibleText(4000)'), the commit message, references/runner-design.md ('The fingerprint's text head is the first 500 chars of the same text ... signature() hashes the same 500 chars') and the requirement in this review's task are all false. Verif

### [should_fix] Warm-request and browser-per-action rows cannot be recomputed from the cited bench JSON  (README.md; reported 1x)
The README opens with 'Every performance number in the README, SKILL.md and references/ comes from scripts/bench.py output committed here', and README.md / SKILL.md cite 2026-09-21-track1-{baseline,after}.json for 'Jev request, warm 770 -> 307 ms' (badpw 751 -> 328) and 'browser per action 646 -> 147 ms' (badpw 642 -> 146). bench.measure_trace stores only per-run sums (jev_ms, browser_ms) and counts, so those rows are not derivable from the committed files: from the JSON one gets jev_ms/requests = 820 -> 448 (first request included) and browser_ms/actions_executed = 743 -> 251. I recomputed from the untracked traces the JSON points at (runs/bench/smoke-login/20260921-234757-0N and 20260922-084345-0N, all 10 present): median latency_ms.jev over steps 2..n = 770 / 307 (badpw 751 / 328) and median latency_ms.browser over action steps = 646 / 147 (badpw 642 / 146); trace.timing gives navigation ~1.97 s and launch ~0.15 s for the README's '2.0 s / 0.15 s'. So the cited numbers are correct but unverifiable from the repo, and a reader who 'opens the JSON and recomputes medians' gets different values. Wall (8,896 -> 5,165, -41.9%; badpw 11,563 -> 6,040, -47.8%), tokens (3,858 -> 5,126), co

### [should_fix] Warm-request and per-action medians are not derivable from the cited JSON  (README.md; reported 1x)
README.md:80 cites `2026-09-21-track1-{baseline,after}.json` for the whole table and measurements/README.md:3 says every number "comes from scripts/bench.py output committed here". The rows 'Jev request, warm' (770/307, 751/328) and 'browser per action' (646/147, 642/146) cannot be recomputed from those files: bench.py stores only per-run sums (`jev_ms`, `browser_ms`) and `step_confidences`, no per-step latency lists. I could only confirm them by opening the uncommitted local traces in runs/bench/ (median of `latency_ms.jev` over steps 2..n, median of `latency_ms.browser` over executed actions), where they match exactly. Wall, tokens, confidence, status counts and every A/B row do reconcile with the JSON. Fix: have bench.py record `step_jev_ms` / `step_browser_ms` lists per run (like `step_confidences`) so the medians are reproducible from the committed file, or state in both READMEs that those two rows come from the per-step `latency_ms` in the traces and how they were computed.

### [should_fix] Warm-request, per-action and timing numbers are not derivable from the cited measurement JSON  (README.md; reported 1x)
measurements/README.md line 3 claims "Every performance number in the README, SKILL.md and references/ comes from scripts/bench.py output committed here" (bench.py's docstring says the same). The committed JSON files only hold per-run sums (jev_ms, browser_ms, requests, tokens) plus decision confidences; they carry no per-step latencies, no first-request latency and no trace.timing. So the rows "Jev request, warm 770 -> 307 ms", "browser per action 646 -> 147 ms", badpw "751 -> 328" / "642 -> 146", README's "about 2.0 s is the initial page load and 0.15 s the browser launch", and SKILL.md's "~0.3 s per decision (~0.8 s first request), ~0.15 s per action, ~2 s page load" cannot be recomputed from the files they cite. What the files do support: wall 8,896 -> 5,165 ms (-42%) and 11,563 -> 6,040 ms (-48%), confidence 0.94 -> 0.95, input tokens 3,858 -> 5,126, statuses 5/5 - all match. Recomputing from the JSON naively gives jev_ms/requests 820 -> 448 ms and browser_ms/actions 743 -> 251 ms, i.e. materially different from the quoted figures. I then recomputed the quoted figures from the uncommitted local traces the JSON's out_dir fields point at (runs/bench/...): warm Jev median 770 -> 

### [should_fix] Waste-management domain vocabulary in fixtures and examples (one added by the reviewed commit)  (unit_tests.py; reported 1x)
Judgment call, flagged because the brief says generic demo content only and the user's note says no employer domain examples: unit_tests.py:519/534 (added by 12fdb1a) uses context "1000-05 Residual Nile"; selftest.py:108-110 has "1000-04-660L / Cardboard 660L / Provas2 Depot" and "1000-05-1100L / Residual 1100L / Nile Bakery"; runner-design.md:120 quotes the same row; SKILL.md:34 and assets/spec.example.json (id orders-create-pickup, 'pickup order', 'Cardboard 660L' container type) use the same flow. Container IDs, waste fractions (Residual/Cardboard), bin sizes (660L/1100L) and a depot are the employer's domain, even without a company name or URL; 'Provas2 Depot' in particular reads like a real site name. Not must_fix since nothing identifies the company, but swapping to neutral nouns (an e-commerce order row: SKU, product, warehouse) removes the question entirely. The row-context test only needs any multi-cell text.

