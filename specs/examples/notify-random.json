{
  "id": "notify-random",
  "start_url": "https://the-internet.herokuapp.com/notification_message_rendered",
  "goal": "Click the 'Click here' link once, so that a notification message about the action appears at the top of the page.",
  "comment": "The failure message is declared `bug` on purpose, to show the computed `flaky` verdict on a page that is random by design. For a real smoke check of this page make both messages `pass` outcomes (the page guarantees a notification, not which one) and keep `text_in` on #flash; see SKILL.md, Suites.",
  "checks": {
    "notification": "A notification message is shown above the heading"
  },
  "outcomes": {
    "action_successful": {
      "when": "A notification above the heading says 'Action successful'",
      "verdict": "pass"
    },
    "action_unsuccessful": {
      "when": "A notification above the heading says the action was unsuccessful and asks to try again",
      "verdict": "bug",
      "note": "this page fails at random by design: across repeats the suite should report it flaky"
    }
  },
  "assert": [
    {
      "url_matches": "**/notification_message_rendered"
    },
    {
      "text_in": {
        "selector": "#flash",
        "contains": "Action successful"
      }
    }
  ],
  "budget": {
    "max_steps": 6,
    "max_seconds": 60
  }
}
